# Movie Catalog App

Приложение "Каталог фильмов", реализованное в рамках тестового задания.
Проект представляет собой SPA (Single Page Application) для поиска и просмотра информации о фильмах и сериалах, использующее реальное API (TMDb).

### 🌐 Демо: [movie-catalog-hazel.vercel.app](https://movie-catalog-hazel.vercel.app/all)

В реализации сделан упор на новейшие возможности **Angular 21**, производительность и UX.

## 🛠 Стек технологий и архитектурные решения

### Core
*   **Framework:** Angular 21 (Latest).
*   **Documentation:** [Angular.dev](https://angular.dev/)
*   **Reactivity:** Полное использование **Angular Signals** (`signal`, `computed`, `effect`, `input`, `output`) для управления состоянием.
*   **Control Flow:** Современный синтаксис шаблонов (`@if`, `@for`, `@switch`).
*   **Routing:** Angular Router (Lazy Loading, Guards, Resolvers, Component Input Binding).

### State Management
*   **Local State Services:** Собственная реализация управления состоянием на базе Signals (без использования тяжеловесных библиотек вроде NgRx), инкапсулированная в сервисах (`MediaListStateService`).

### UI / UX & Styling
*   **Styling:** SCSS с использованием методологии **BEM** и CSS Custom Properties (Variables).
*   **Responsive:** Полная адаптивность (Desktop / Tablet / Mobile) с использованием `mixin` (Desktop-first подход).
*   **Optimization:**
  *   Стратегия `OnPush` во всех компонентах.
  *   **Infinite Scroll** на базе `IntersectionObserver API`.
  *   **Skeleton Loaders** для улучшения восприятия загрузки.
  *   **NgOptimizedImage** (`ngSrc`) для оптимизации загрузки изображений (LCP).
  *   Сервис блокировки скролла (`ScrollLockService`) при открытии модальных окон.

### API & Data
*   **Backend:** Интеграция с [The Movie Database (TMDb) API](https://www.themoviedb.org/documentation/api).
*   **HTTP:** `HttpClient` (с `withFetch`) и функциональными интерцепторами (`authInterceptor`) для автоматической подстановки токенов.

### Testing
*   **Unit Testing:** Vitest (вместо Karma/Jasmine) для высокой скорости запуска тестов.

---

## ✨ Реализованный функционал

> **Примечание:** Функционал расширен относительно базового ТЗ для демонстрации навыков построения масштабируемых приложений на актуальном стеке.

1.  **Каталог:**
  *   Разделение на "Фильмы", "Сериалы" и "Все".
  *   Сортировка (Новинки, Лучшее).
  *   Фильтрация по жанрам (через сайдбар).
  *   Бесконечная прокрутка списка.
2.  **Поиск:**
  *   "Живой" поиск с Debounce.
  *   Сохранение состояния поиска и фильтров в URL (Shareable Links).
3.  **Просмотр деталей:**
  *   Модальное окно для быстрого просмотра.
  *   Отдельная детальная страница (Routing).
  *   Просмотр трейлеров (YouTube Integration).
  *   Список актеров и рекомендации.
4.  **Адаптивность:**
  *   Полноценный Sidebar для мобильных устройств.
  *   Адаптивная сетка карточек.

---

## 🗺 Roadmap (Планы по развитию)

Планируется дальнейшее развитие проекта и внедрение следующего функционала:

### UI/UX и Контент
*   [ ] **Вывод режиссера:** Отображение имени режиссера на странице медиа.
*   [ ] **Рефакторинг списков:** Пересмотр концепции "Общего списка медиа" (разделение потоков).

### Расширенная фильтрация
*   [ ] **Фильтр по режиссеру.**
*   [ ] **Фильтр по актерам.**
*   [ ] **Фильтр по рейтингу.**
*   [ ] **Фильтр по годам:** Выбор диапазона дат (range).
*   [ ] **Фильтр по странам.**
*   [ ] **Фильтр по количеству комментариев.**

### Пользовательские функции
*   [ ] **Аутентификация:** Регистрация и вход по электронной почте.
*   [ ] **Избранное:** Возможность добавлять фильмы и сериалы в список для просмотра.

---

## 🚀 Запуск проекта

Для запуска вам понадобится [Node.js](https://nodejs.org/) (рекомендуется LTS версия).

1.  **Установка зависимостей:**
    ```bash
    npm install
    ```

2.  **Запуск локального сервера:**
    ```bash
    npm start
    # или
    ng serve
    ```
    Приложение будет доступно по адресу `http://localhost:4200/`.

3.  **Запуск тестов:**
    ```bash
    npm test
    ```

---

## 📝 Ответы на теоретические вопросы

Ответы на вопросы из второй части тестового задания находятся в файле ANSWERS.md в корне проекта.
